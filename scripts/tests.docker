set -euxo pipefail
sudo docker run --rm -it ubuntu:14.04  /bin/bash -c "apt update && apt install -y git build-essential  zlib1g zlib1g-dev && git clone --recurse-submodules  https://github.com/quadram-institute-bioscience/lotus-tools.git && bash ./lotus-tools/scripts/make_all.sh" 2>/dev/null
sudo docker run --rm -it ubuntu:16.04  /bin/bash -c "apt update && apt install -y git build-essential  zlib1g zlib1g-dev && git clone --recurse-submodules  https://github.com/quadram-institute-bioscience/lotus-tools.git && bash ./lotus-tools/scripts/make_all.sh" 2>/dev/null
sudo docker run --rm -it ubuntu:18.04  /bin/bash -c "apt update && apt install -y git build-essential  zlib1g zlib1g-dev && git clone --recurse-submodules  https://github.com/quadram-institute-bioscience/lotus-tools.git && bash ./lotus-tools/scripts/make_all.sh" 2>/dev/null
sudo docker run --rm -it alpine  /bin/sh -c         "apk add git make g++ zlib-dev bash &&  git clone --recurse-submodules https://github.com/quadram-institute-bioscience/lotus-tools.git && bash ./lotus-tools/scripts/make_all.sh" 2>/dev/null
